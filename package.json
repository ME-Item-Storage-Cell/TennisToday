{
  "dependencies": {
    "@supabase/supabase-js": "^2.107.0",
    "react-router-dom": "^7.17.0",
    "styled-components": "^6.4.2"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "gh-pages -d dist",
    "build:gh-pages": "vite build && gh-pages -d dist"
  }
}
